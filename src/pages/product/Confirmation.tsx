import React, { useEffect, useRef, useState } from 'react';

const Confirmation = ({
  total,
  currencySymbol,
  setShowConfirmation,
  readyBy,
  name,
  selectedParts,
  selectedPrintOn,
  selectedSize,
  modelImage,
  customSizeValues,
  height,
  gender,
  uploadedImageLeft,
  uploadedImageRight,
  textLeft,
  textRight,
}) => {
  return <div></div>;
};

export default Confirmation;
